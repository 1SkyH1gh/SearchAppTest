import * as React from 'react'

export const Cancel=()=>{
    return(
        <button className="Cancel">
            <h2 className="CancelText">
            Cancel
            </h2>
        </button>

    )
}