import * as React from 'react'

export const Cancel=()=>{
    return(
        <button className="Cancel">
            <p className="CancelText">
            Cancel
            </p>
        </button>

    )
}